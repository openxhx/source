namespace beachDeliveryMan {
    export const enum BeachActivityTime {
        /**还没开始 */
        none_start = "none_start",
        /**进行中 */
        inner_doing = "inner_doing",
        /**活动结束 */
        finished = "finished"
    }
}