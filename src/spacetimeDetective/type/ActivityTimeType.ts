namespace spacetimeDetective {
    export const enum ActivityTimeType {
        /**还未开始 */
        NONE_START = "NONE_START",
        /**游戏中 */
        GAMEING = "GAMEING",
        /**结束 */
        OVER = "OVER"
    }
}